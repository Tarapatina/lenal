1) cd (change directori) просто переходим в другую директорию
2) ls выводит все, что есть в папке
3) pwd -это функция. Текущая директория, где мы сейчас находимся. ДИРЕКТОРИЯ -это папка!
4) git ( это функция) init (АРГУМЕНТ) Это я хочу инициализировать (создать) репозиторий.
5) сd        . текущая директория   .. директория уровня выше
6) mkdir "название папки" -это если мы хотим создать новую папку
7) cp - функция. Сокращение от copy. должно быть 2 аргумента. Например: c:desktop\file1 folder
есть полный и относительный путь
8) rm - удалить файл
